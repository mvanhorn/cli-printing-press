# {{.DisplayName}}{{if not .DisplayName}}{{.APIName}}{{end}}
{{if and .Narrative .Narrative.Headline}}
**{{.Narrative.Headline}}**
{{end}}{{if and .Narrative .Narrative.ValueProp}}
{{.Narrative.ValueProp}}
{{else if .Description}}
{{.Description}}
{{end}}
## Install

### Go

```
go install {{.ModulePath}}/cmd/{{.CLIName}}@latest
```

### Binary

Download from [Releases](https://github.com/mvanhorn/printing-press-library/releases).

## Authentication
{{if and .Narrative .Narrative.AuthNarrative}}
{{.Narrative.AuthNarrative}}
{{else if eq .AuthType "api_key"}}
Set your API key:

```bash
{{range .AuthEnvVars}}export {{.}}="your-key-here"
{{end}}{{.CLIName}} auth set-token
```

Verify: `{{.CLIName}} doctor`
{{else if eq .AuthType "bearer_token"}}
Store your bearer token:

```bash
{{.CLIName}} auth set-token YOUR_TOKEN_HERE
```
{{else if eq .AuthType "oauth2"}}
Authenticate via browser:

```bash
{{.CLIName}} auth login
```
{{else if or (eq .AuthType "cookie") (eq .AuthType "composed")}}
This CLI uses a browser session. Log in via Chrome, then:

```bash
{{.CLIName}} auth login
```
{{else}}
No authentication required.
{{end}}
## Quick Start
{{if and .Narrative .Narrative.QuickStart}}
```bash
{{range .Narrative.QuickStart}}{{if .Comment}}# {{.Comment}}
{{end}}{{.Command}}

{{end}}```
{{else}}
```bash
# Verify setup
{{.CLIName}} doctor
{{if .BaseCommands}}
# Try your first command
{{.CLIName}} {{(index .BaseCommands 0).Path}} --help
{{end}}```
{{end}}
{{if .TranscendenceCommands}}## Unique Features

These capabilities aren't available in any other tool for this API.
{{$groups := groups .TranscendenceCommands}}{{if gt (len $groups) 1}}{{range $groups}}
### {{.Name}}
{{range .Commands}}
- **`{{.Path}}`** — {{.Short}}{{if .WhyItMatters}}

  _{{.WhyItMatters}}_{{end}}{{if .Example}}

  ```bash
  {{.Example}}
  ```{{end}}
{{end}}{{end}}{{else}}{{range .TranscendenceCommands}}
- **`{{.Path}}`** — {{.Short}}{{if .WhyItMatters}}

  _{{.WhyItMatters}}_{{end}}{{if .Example}}

  ```bash
  {{.Example}}
  ```{{end}}
{{end}}{{end}}

{{end}}## Commands
{{range .BaseCommands}}
- **`{{.Path}}`** — {{.Short}}
{{end}}

## Agent Usage

Add `--agent` to any command. Expands to `--json --compact --no-input --no-color --yes` for structured, token-efficient output.

Exit codes: `0` success, `2` usage error, `3` not found, `4` auth error, `5` API error, `7` rate limited.
{{if .Sources}}
## Sources & Inspiration

This CLI was built by studying these projects:
{{range .Sources}}
- [**{{.Name}}**]({{.URL}}){{if .Language}} — {{.Language}}{{end}}{{if .Stars}} ({{.Stars}} stars){{end}}
{{- end}}
{{end}}
Generated by [CLI Printing Press](https://github.com/mvanhorn/cli-printing-press)
